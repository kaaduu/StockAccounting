# Prefer Java 17 for compilation.
# The wrapper still works on newer JDKs, but using JDK 17 avoids toolchain warnings and
# ensures --release 17 is supported.
org.gradle.java.home=/usr/lib/jvm/java-17-openjdk-amd64
