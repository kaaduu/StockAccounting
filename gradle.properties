# Prefer Java 17 when available.
#
# Note: This property is machine-specific. If unset, Gradle uses the current JVM.
# Keeping the build portable is preferred; configure JAVA_HOME or org.gradle.java.home
# locally if needed.

# Keep logs cleaner on CI.
org.gradle.warning.mode=all
